services:
  web:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped
    depends_on:
      - api

  api:
    build: ./backend
    ports:
      - "8000:8000"
    restart: unless-stopped
