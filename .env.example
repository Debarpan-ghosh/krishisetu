# Copy to .env and fill in for local dev.
# Currently unused since the app runs on mocked data —
# wire these up once a real backend/API exists.

VITE_API_BASE_URL=http://localhost:4000
VITE_MANDI_PRICE_API_KEY=
VITE_ESCROW_PROVIDER_KEY=
